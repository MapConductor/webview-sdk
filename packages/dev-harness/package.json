{
  "name": "@mapconductor/webview-dev-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Plays the role of native: drives @mapconductor/webview-runtime (hosted in an iframe) over the same postMessage protocol a real WebView bridge would use",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.20"
  }
}
